# Knowledge Graph Edge Index

`knowledge_graph_edges.jsonl` provides machine-readable relationships between lanes.

Use cases:
- GraphAtlas ingestion
- query expansion
- answer routing
- conflict discovery
- topic expansion planning

The graph is intentionally lightweight. It does not replace lane content; it helps the AI find adjacent lanes when a question spans multiple domains.
