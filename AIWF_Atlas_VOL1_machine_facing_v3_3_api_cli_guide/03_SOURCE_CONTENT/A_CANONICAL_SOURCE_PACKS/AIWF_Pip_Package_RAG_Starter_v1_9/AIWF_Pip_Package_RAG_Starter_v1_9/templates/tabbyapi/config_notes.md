# TabbyAPI config notes

Capture these before support:

- TabbyAPI commit/version
- ExLlamaV2 version
- Torch/CUDA version
- model path
- model format/quant branch
- config.yml
- server startup log
- first failed request JSON
