# AIWF Wheelhouse

This folder is for local compiled/cached wheels, manifests, and smoke-test evidence.

The archive includes only templates and docs. Do not place large wheel binaries in the RAG archive unless the user explicitly wants a wheelhouse artifact archive.

Use `wheel_build_candidates.csv` to prioritize which packages deserve local wheel treatment.
