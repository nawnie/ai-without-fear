# Theming and Custom Components

Gradio 6 offers much stronger theming capabilities through CSS variables and full support for custom components built with Svelte 5.

The `gradio cc` CLI makes it significantly easier to create, develop, and publish custom components compared to previous versions.