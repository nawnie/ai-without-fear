# Creating Custom Components

Use the `gradio cc create` command to scaffold a new custom component. Development is done with `gradio cc dev` for hot reloading.

After building with `gradio cc build`, you can publish components to the Hugging Face Component Gallery for reuse across projects.