# gr.Server() Mode

`gr.Server()` allows you to keep the Gradio backend while bringing your own frontend (React, Vue, Svelte, etc.).

This is useful when you need full control over the UI or want to embed Gradio functionality into an existing web application.