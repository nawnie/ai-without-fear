# Caching for Generation Apps

Use `@gr.cache` aggressively on expensive generation steps when inputs are repeated. Combine exact caching with semantic caching where appropriate to reduce cost and latency.