# Caching Strategies

The new `@gr.cache` decorator makes it easy to cache expensive functions. Combine it with `gr.Cache` for more advanced control. Cache hits dramatically improve response time and reduce cost.