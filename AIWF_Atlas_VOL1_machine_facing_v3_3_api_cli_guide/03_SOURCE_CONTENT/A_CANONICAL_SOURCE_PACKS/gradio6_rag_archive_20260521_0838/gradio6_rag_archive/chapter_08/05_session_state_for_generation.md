# Session State for Generation Workflows

Use `gr.State()` or `gr.BrowserState()` to persist generation parameters and history within a session. For multi-user or scaled deployments, move important state to external storage.