# Dataframe and Gallery Components

The Dataframe component received improvements in Gradio 6 for better editing and display. The Gallery component is useful for showing multiple images with support for selection and captions.

Both components work well with caching when the underlying data generation is expensive.