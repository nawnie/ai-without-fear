# Python and JavaScript Clients

Gradio 6 apps can be called from Python using `gradio_client` and from JavaScript using `@gradio/client`. This turns your UI into a reusable API for other services and agents.