# Blocks and the Event System Overview

`gr.Blocks` is the core layout system in Gradio. It allows you to build flexible UIs using rows, columns, tabs, and accordions.

The event system in Gradio 6 has been significantly improved with clearer chaining using `.then()`, `.success()`, and `.failure()`. This makes complex workflows much easier to manage and debug.