# Building Chatbots in Gradio 6

Gradio 6 has improved support for chat interfaces with better streaming, multimodal input, and structured message formats.

The recommended way to build chatbots is using `gr.ChatInterface` or custom `gr.Blocks` with the new `Chatbot` component (`type="messages"`).