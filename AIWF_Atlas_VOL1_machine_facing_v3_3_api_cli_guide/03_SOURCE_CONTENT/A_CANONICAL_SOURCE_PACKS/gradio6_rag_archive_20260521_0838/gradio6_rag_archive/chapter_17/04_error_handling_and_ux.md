# Error Handling and User Experience

Good production apps handle errors gracefully. Use `gr.Error()` for user-friendly messages, show progress during long tasks, and support partial results when full processing fails.