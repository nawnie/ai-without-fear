# Concurrency and Queue Tuning

Use `concurrency_limit` to control how many expensive events can run at the same time. Set `max_size` on queues to prevent overload. Use `queue=False` for instant UI updates.