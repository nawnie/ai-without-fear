# ComfyUI-WanVideoWrapper — Pass 02B Deep Schema

`WanVideoSampler` is now indexed as schema-confirmed but expert/local-test only.

AIWF rule: do not build beginner workflows around WanVideoWrapper until the exact model-loading, text/image embeds, VAE/decode, context/cache, and output path are audited together.

The sampler is valuable for RAG repair because it exposes required video sampling arguments and many optional integration paths.
