# RAG Nodepack 045 — SDXL Prompt Styler — PASS 02F Light Coverage

- Repository: `twri/sdxl_prompt_styler`
- Coverage mode: light/default; dense only if this pack becomes first-choice capability.
- Rows captured: 2

## AIWF use

- `SDXLPromptStyler` — Apply reusable SDXL style templates to prompt text. Policy: `safe_generate_after_user_choice`. Risks: `style_template,sdxl_prompting`.
- `SDXLPromptStylerAdvanced` — Apply SDXL dual-encoder style templates. Policy: `safe_generate_after_user_choice`. Risks: `style_template,sdxl_dual_encoder`.

## Notes
This pass intentionally favors broad coverage and overlap mapping over exhaustive socket capture. Use source_refs in the CSV for deeper follow-up.